/*var Recruiter = artifacts.require("./Recruiter.sol");

 it("it initializes the recruiter with the correct values", function() {
 	var electionInstance;
    return Recruiter.deployed().then(function(instance) {
      electionInstance = instance;
      return electionInstance.RecruiterId(1);   // mapping variable
    }).then(function(Recruiter) {
      assert.equal(Recruiter[0], "my", "correct username");
	  assert.equal(Recruiter[1], "code", "correct passworc");
	  assert.equal(Recruiter[2],"hekko","")
          

      return electionInstance.Recruiter(1);
    });
  });
});*/